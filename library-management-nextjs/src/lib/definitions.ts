import type { IBook, IBookBase } from "./models/book.model";
import type { IMember, IMemberBase } from "./models/member.model";
import type {
  ITransaction,
  ITransactionBase,
} from "./models/transaction.model";
export type {
  IBook,
  IBookBase,
  IMember,
  IMemberBase,
  ITransaction,
  ITransactionBase,
};
